name: Sync Almaty People

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24

      - run: node scripts/sync-almaty.js

      - name: Commit updated data
        run: |
          git config user.name "sync-bot"
          git config user.email "sync-bot@example.com"
          git add data/almaty-people.json
          git commit -m "Update Almaty people data" || echo "No changes"
          git push
